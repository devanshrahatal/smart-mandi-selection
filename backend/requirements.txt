# ============================================================
# Smart Mandi Selection — Backend Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database (PostgreSQL + ORM + Migrations) ---
sqlalchemy==2.0.35
alembic==1.13.2
pymysql==1.1.1
cryptography==43.0.1  # required by pymysql for secure MySQL connections

# --- Redis ---
redis==5.1.0

# --- Configuration ---
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Auth (JWT + password hashing) ---
python-jose[cryptography]==3.3.0
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0

# --- HTTP client (for Agmarknet & Google Maps APIs) ---
httpx==0.27.2

# --- WhatsApp bot (Twilio) ---
twilio==9.3.0

# --- Background job scheduler ---
apscheduler==3.10.4

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx  # also used as async test client via TestClient

# --- Voice AI & Speech Synthesis ---
gTTS==2.5.4

# --- Utilities ---
python-multipart==0.0.9  # required by FastAPI for form data (Twilio webhooks)
pyngrok==7.2.2
